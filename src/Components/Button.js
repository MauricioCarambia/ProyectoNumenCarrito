import React from 'react'

const Button = () => {
    return (
        <button style={boton}>Agregar al carrito</button>
    )
}

export default Button

const boton = {
    backgroundColor: "royalblue",
    border: "none",
    borderRadius: "5px",
    color: "white",
    padding: "5px 10px",

}